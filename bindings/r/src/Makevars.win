# On Windows the C ABI DLL is linked directly (mingw resolves the import stubs
# from wickra_genome.dll); the loader finds it via PATH at run time.
PKG_CPPFLAGS = -I$(WKGENOME_INC)
PKG_LIBS = -L$(WKGENOME_LIB) -l:wickra_genome.dll
